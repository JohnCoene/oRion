# Sample .travis.yml for R projects

language: r

warnings_are_errors: 1

sudo: required

r_packages:
  - covr
  - jsonlite
  - httr
  - plyr
  - methods

after_success:
  - Rscript -e 'covr::codecov()'
  - Rscript -e 'covr::coveralls()'
